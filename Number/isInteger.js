// The Number.isInteger() Method

// The Number.isInteger() method returns true if the argument is an integer.

var num = 7;

document.write(
    Number.isInteger(num) + "<br>" + Number.isInteger(10.5)
)