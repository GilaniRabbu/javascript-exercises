// The Number.parseFloat() Method

// Number.parseFloat() parses a string and returns a number.

// If the number cannot be converted, NaN (Not a Number) is returned.

var num = "7.98";

document.write(
    Number.parseFloat(num) + "<br>" + Number.parseFloat("10.33")
)